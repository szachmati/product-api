spring:
  # Below properties are filled in BaseIntegrationTest class
  datasource:
    driver-class-name:
    url:
    username:
    password:
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    show-sql: true